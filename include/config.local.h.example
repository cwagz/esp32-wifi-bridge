/*
 * Optional local compile-time overrides. Gitignored.
 *
 * GitHub forks do not need this file for OTA: tagged CI and local builds
 * derive https://<you>.github.io/<repo>/version.json from the git remote.
 *
 * Copy this to config.local.h only if you host firmware somewhere else
 * or want compile-time defaults that differ from config.h.
 *
 *   cp include/config.local.h.example include/config.local.h
 */

#pragma once

/* #define REMOTE_OTA_VERSION_URL "https://example.com/version.json" */

/* Optional compile-time WiFi defaults (the dashboard still wins at runtime). */
/* #define WIFI_SSID "TeslaPowerwall" */
/* #define WIFI_PASSWORD "" */

/* #define MDNS_HOSTNAME "powerwall" */
/* #define ADMIN_USERNAME "admin" */
