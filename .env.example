# Copy to .env (gitignored) for local builds.
# CI sets this automatically from github.repository_owner on tagged releases.
#
# Do not put secrets here — this value is compiled into the firmware image.

REMOTE_OTA_VERSION_URL=https://YOUR_GITHUB_USER.github.io/esp32-wifi-bridge/version.json
